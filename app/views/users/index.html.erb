<% provide(:title, @title) %>
<h1><%= @title %></h1>
<h1>Hello World</h1>

<%= render 'users/search_form' %>

<%= will_paginate @users %>

<ul class='users'>
    <%= render @users %>
</ul>

<%= will_paginate %>
